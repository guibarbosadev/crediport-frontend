export interface ITheme {
    color: {
        primary: string;
        gray: string;
    };
}

export const theme: ITheme = {
    color: {
        primary: '#4361ee',
        gray: '#939aa9'
    }
};
